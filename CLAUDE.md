# CLAUDE.md — הנחיות לפרויקט התראות תנועה לאצטדיון טדי

מסמך זה מנחה כל סוכן/מפתח שעובד בריפו. **כלל-העל: לעולם לא לדחוף מידע אישי או
סודות.** הריפו ציבורי באופיו — הקבצים מכילים workflow מנוקה בלבד.

---

## 🔐 כללי אבטחה (חובה — לפני כל commit / push)

1. **אסור לדחוף סודות.** מפתחות API, טוקנים, סיסמאות ו-OAuth tokens נשמרים
   **אך ורק** כ-Credentials בתוך n8n — לעולם לא בקבצי הריפו.
   - API-Football → Header Auth (`x-apisports-key`)
   - Telegram → bot token
   - Gmail → OAuth2

2. **אסור לדחוף מידע אישי מזהה (PII).** לפני כל קומיט החלף ערכים אמיתיים בתבניות:
   | שדה | מקור | להחליף ל- |
   |------|------|-----------|
   | `chatId` (Telegram) | נוד `Send a text message` | `YOUR_TELEGRAM_CHAT_ID` |
   | `sendTo` (Gmail) | נוד `Send a message` | `your-email@example.com` |
   | `webhookId` | נודי Telegram/Gmail | להסיר לחלוטין |

3. **כתובות/קואורדינטות** — מותרות רק אם הן של אצטדיון טדי (מידע ציבורי).
   אסור לכלול קואורדינטות של בית/עבודה.

4. **כשמייצאים workflow מחדש מ-n8n** — הייצוא הגולמי מכיל את הערכים האישיים
   האמיתיים. תמיד לנקות אותו לפני שמירה ל-`workflow/`. אל תדחוף קבצי ייצוא גולמיים
   (`.gitignore` חוסם `*.raw.json`, `credentials*.json`, `export/`).

5. **בדיקה לפני push** — הרץ סריקה ווודא שאין דליפות:
   ```bash
   git diff --cached | grep -iE 'apisports|bot[0-9]+:|@gmail|[0-9]{9,}' || echo "clean"
   ```
   מומלץ גם `gitleaks detect` אם מותקן.

6. **בספק — לא לדחוף.** אם לא ברור אם ערך הוא רגיש, התייחס אליו כרגיש ושאל.

---

## 📁 מבנה ועקרונות

- `workflow/teddy-stadium-alerts.json` — המקור היחיד של ה-workflow (מנוקה).
- `docs/ARCHITECTURE.md` — פירוט nodes + צ׳קליסט אבטחה מלא.
- שינויים בלוגיקה (זמני נסיעה, מקורות, תזמון) — לעדכן גם את ה-JSON וגם את התיעוד.
- ערכים שמשתנים בין משתמשים מתועדים ב-`.env.example` — לעדכן אותו כשמוסיפים סוד/הגדרה.

## 🌿 Git

- לפתח על branch ייעודי, לא על default.
- commit/push רק כשמתבקש במפורש.
- לא לפתוח PR ללא בקשה מפורשת.
